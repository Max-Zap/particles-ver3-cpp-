# particles-ver3-cpp-
This is a particle simulation using C++ which uses a 2d array of 16bit unsigned integers. The 16bit values use a preset set of rules to move, combine, and divide into smaller values. The exact system used is explained further in the README
